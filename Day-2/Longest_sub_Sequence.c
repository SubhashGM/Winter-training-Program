#include<stdio.h>
int main()
{
    int test;
    while(test)
    {
        int n,j,i,a[100];
        scanf("%d",&n);
        for(i=0;i<n;i++)
            scanf("%d",&a[i]);
        int curr_length=1;
        int max_length=1;
        int start=0,max_start=0,max_end=0;
        int end=0;
        for(j=1;j<=n,j++)
        {
            if(a[j]>=a[j-1])
            {


            }
        }
        test--;
    }
}
